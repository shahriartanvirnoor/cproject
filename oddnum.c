#include<stdio.h>
int main()
{
    int n;
    printf("Enter the number till you want to get the some: ");
    scanf("%d", &n);
    int result = n*n;
    printf("First nth odd number's sum : %d", result);
    
}